# Data Folder

Place the churn dataset here before running the notebook.

Expected file name:

```text
bank_churn_data.csv
```

The CSV file is ignored by Git to avoid uploading local or potentially restricted data.
